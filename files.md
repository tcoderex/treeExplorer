# Important Project Files

- `index.html`: Contains the UI layout, including settings panel and modals.
- `src/ui.js`: Contains event listeners for buttons and toggles.
- `src/canvas.js`: Contains `LineageCanvas` class which controls rendering. Will be modified to support a completely new `isGenealogyMode`.
- `package.json`: Contains project metadata and version number.
